# 不设置 VITE_API_BASE → 使用相对路径 /api，由 Vite 代理到本机 8080（推荐）。
# 用手机/平板访问 http://电脑IP:5173 时也必须如此：若写 localhost，请求会发到手机本机而非你的电脑。
# 仅在本机浏览器且想绕过代理直连后端时，可临时改为：
# VITE_API_BASE=http://127.0.0.1:8080/api
# 切勿写成 VITE_API_BASE=/ 或其它非 /api 的相对路径，否则会出现 “No static resource plaza/...”。
